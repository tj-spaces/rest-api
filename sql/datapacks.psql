DROP TABLE IF EXISTS "datapacks";

CREATE TABLE "datapacks" (
	"id" BIGINT,
	"creator_id" BIGINT,
	"name" VARCHAR(255),
	"description" VARCHAR(1023),

	CONSTRAINT "fk_datapack__creator"
		FOREIGN KEY ("creator_id")
			REFERENCES "users"("id")
				ON DELETE CASCADE,

	PRIMARY KEY ("id")
)