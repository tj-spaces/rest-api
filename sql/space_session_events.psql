DROP TABLE IF EXISTS "space_session_events";

CREATE TABLE "space_session_events" (
	"space_id" BIGINT,
	"sequence_number" INTEGER,
	"time" TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
	"type" VARCHAR(255),
	"metadata" JSON,

	PRIMARY KEY ("space_id")
)